import { Meta } from '@storybook/addon-docs/blocks';


<Meta title="Components/更新日志" />


# 更新日志
---- 

- 修订版本号：每周末会进行日常 bugfix 更新。（如果有紧急的 bugfix，则任何时候都可发布）
- 次版本号：每月发布一个带有新特性的向下兼容的版本。
- 主版本号：含有破坏性更新和新特性，不在发布周期内。

## v0.0.3
`2020-12-14` 

- 🐞 补充`download`组件的单元测试
- 🛠 优化`upload`组件，支持`antd`的`upload`的属性

## v0.0.2
`2020-12-11` 

- 🆕 新增 `better-input-number` 组件。
- 🆕 新增 `block` 组件。
- 🆕 新增 `download` 组件。
- 🆕 新增 `once-Skeleton` 组件。
- 🆕 新增 `upload` 组件。


## v0.0.1
`2020-12-7` 

添加`friday-components`组件包，串通了开发，测试，打包，文档所有流程。

- 支持 TypeScript
- 支持 单元测试 Jest + @testing-library/react
- 支持 less
- 支持 mdx 文档